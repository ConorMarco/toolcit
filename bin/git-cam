#!/usr/bin/env bash
# git-cam - stage all changes and commit with the given message
#
# Usage: git cam <message>

set -euo pipefail

if [[ $# -eq 0 ]]; then
    echo "Usage: git cam <message>" >&2
    exit 1
fi

git add -A
git commit -m "$*"
